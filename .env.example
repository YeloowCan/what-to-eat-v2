# Copy to .env and fill in. Taro injects TARO_APP_* vars into the bundle.
# These are CLIENT-SIDE secrets (the adapter calls Amap directly). See
# src/adapters/amapPoiSource.ts for the security trade-off vs the PRD's
# appid-whitelist scheme.

TARO_APP_AMAP_KEY=
TARO_APP_AMAP_SECRET=
